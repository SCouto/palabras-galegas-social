# palabras-galegas-social

Rendered post images for the Palabras Galegas Instagram account.

This repo is public for one reason: the Instagram Content Publishing API takes an image_url and fetches it from Meta's own servers — there is no binary upload for feed images. The pictures have to be anonymously reachable over HTTPS.
